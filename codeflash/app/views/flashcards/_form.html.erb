<%= form_for(@flashcard) do |f| %>
  <% if @flashcard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flashcard.errors.count, "error") %> prohibited this flashcard from being saved:</h2>

      <ul>
      <% @flashcard.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.label :description, "Description" %>
  <%= f.text_area :description %>
  <%= f.fields_for :answers do |answer| %>
    <%= answer.label :code, "Code" %>
    <%= answer.text_field :code %>
  <% end %>
  <%= f.label :result, "Result" %>
 <%= f.text_area :result %> 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
